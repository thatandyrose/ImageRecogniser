- flash.each do |name, msg|
  .messages.alert class="alert-#{name.to_s == 'notice' ? 'success' : 'danger'}"
    button.close[type="button" data-dismiss="alert" aria-hidden="true"] ×
    = content_tag :div, msg, :id => "flash_#{name}"
